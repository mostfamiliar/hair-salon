<!DOCTYPE html>
<html>
    <head>
        <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/css/materialize.min.css">
        <link rel="stylesheet" href="/css/styles.css">
        <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/js/materialize.min.js"></script>
        <title>Hair Salon</title>
    </head>
    <body>
        <div class="container">
        <h1><i>HANDSOME DEVIL<i class="material-icons">store</i></i></h1>
            <div class="row">
                <h3>{{ stylist.getName}}</h3>
                <h4>Add a client</h4>
                <form action='/clients' method='post'>
                    <input id="stylist_id" name="stylist_id" type="hidden" value="{{ stylist.getId }}">
                    <label for='client_name'>Client Name</label>
                    <input id='client_name' name='client_name' type='text'>
                <div class="input-field col s12">
                  <button type="submit" class="waves-effect waves-light btn indigo"> <i>SUBMIT<i></button>
                 </div>
            </div>
            </form>

        <div class="row">
            {% if clients is not empty %}
        <p>Here are the clients:</p>
            <ul>
                {% for client in clients %}
                <li><h4>{{ client.getName }}</h4>
                </li>
                <form action="clients/{{client.getID}}/edit" method="get" class="col s12">
                <button type="submit" class="waves-effect waves-light btn indigo lighten-3"> <i>EDIT<i></button>
                </form>
                {% endfor %}
            </ul>
            {% endif %}
        </div>
        <form action="/delete_all_clients" method="post" class="col s12">
        <button type="submit" class="waves-effect waves-light btn red"> <i>DELETE ALL CLIENTS<i></button>
        </form>
           </div>
        </div>
    </body>
</html>
